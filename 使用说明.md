# 使用说明

## 第一次用某个案件之前

打开 `~/法律脱敏工作台/案件_<你填的案件名>/案件人员清单.txt`
(第一次处理这个案件后会自动生成),把本案涉及的姓名、公司名填进去,
一行一个。这一步能明显提高准确率,建议每个新案件都先做。

## 日常使用(两步,常见情况下其实只有第一步)

1. 把这个案子的卷宗文件(PDF 或 docx)拖到「导入新卷宗」这个 App 图标上,
   输入一下案件名称(同一个案件每次填一样的名字),等一会。
   - 如果没有发现需要人工看一眼的内容,会直接告诉你文件已经导出好了,
     可以去 `03_已批准可上传` 文件夹取用。**到这一步就完了。**
   - 如果有需要确认的地方,会提示你先打开审核报告看看。

2. (只有被提示要确认时才需要这一步)打开提示里说的审核报告看一眼:

   - 如果报告里有「OCR 核对」这一节,说明扫描件里有几页识别质量不高,
     报告会指出具体是哪一页、可疑的文字是什么,并给出对应的原图文件名
     (在 `01_文本化` 文件夹里)。**打开那张原图,对照检查报告里列出的
     可疑文字有没有认错字**——扫描件模糊时,AI 有可能把身份证号或姓名
     里的个别字认错,这一步就是用来抓这种错误的。
     - 如果确认没问题,可以直接批准导出(见第 3 步)。
     - 如果发现认错了,打开 `01_文本化` 里同名的 `.txt` 文件,直接把
       认错的地方改成正确的文字,存盘,然后把这个改好的 `.txt` 文件
       重新拖到「导入新卷宗」上(案件名称填得和之前一样),用改正后的
       文字重新处理一遍。
   - 报告里也可能会提示姓名/公司名之类的内容需要确认(而不是 OCR 问题)
     ——直接看候选脱敏文本里替换得对不对就行,不需要对照原图。

3. 确认没问题后,双击「批准并导出」,输入案件名称,文件就会导出到
   `03_已批准可上传`。

## 唯一的规则

**只把 `03_已批准可上传` 文件夹里的文件复制给 AI(Claude 等)。**
其他文件夹(原始文件、转换后的文本、候选脱敏文本)不要上传。
