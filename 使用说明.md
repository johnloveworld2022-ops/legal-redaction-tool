# 使用说明

## 第一次用某个案件之前

在网页里把这个案件涉及的姓名、公司名填进「案件人员清单」(第一次处理
该案件后,案件文件夹里会自动生成这个文件,也可以直接在
`~/法律脱敏工作台/案件_<你填的案件名>/案件人员清单.txt` 里编辑),
一行一个。这一步能明显提高准确率,建议每个新案件都先做。

## 日常使用

1. 双击「打开脱敏工具」这个 App,会自动打开浏览器网页。
2. 填案件名称(同一个案件每次填一样的名字),把卷宗文件(PDF/Word/
   已改好的 txt)拖进网页里的框,或点击选择文件,点击「开始处理」。
3. 处理完自动跳到这个案件的页面:
   - 如果没有发现需要人工看一眼的内容,页面会显示「全部文件已导出」,
     可以去 `03_已批准可上传` 文件夹取用,**到这一步就完了**。
   - 如果有需要确认的地方,页面会列出每份文档的详情:
     - 如果是 OCR 识别质量不高,页面上会直接显示对应的原始页面图片,
       图片下面是一个可以编辑的文字框,里面是识别出来的文字——对照
       图片检查有没有认错字,**发现错字直接在文字框里改**,改完点
       「保存修改并重新处理这份文件」即可,不需要再去找文件、开
       其他软件。
     - 也可能是姓名/公司名之类需要确认,直接展开「查看当前脱敏结果」
       看看替换得对不对就行。
   - 确认所有文档都没问题后,点击底部的「确认无误,批准导出全部」,
     文件就会导出到 `03_已批准可上传`。

## 唯一的规则

**只把 `03_已批准可上传` 文件夹里的文件复制给 AI(Claude 等)。**
其他文件夹(原始文件、转换后的文本、候选脱敏文本)不要上传。
