# 脱敏规则说明

本文档整理这个工具"识别什么、怎么识别、识别不到什么"的具体规则,
供审查和日常使用参考。**这份文档只是整个安全方案的一半**——另一半是
"规则会漏检"这件事本身催生出的强制人工审核机制。只看规则容易误以为
"规则列全了就安全",实际可靠性很大程度来自这些审核机制兜底。两块都
写在这里,后半部分同样要看。

## 一、四层检测,各自负责什么

### 1. 正则规则(源码:`core/detectors_regex.py`)

结构固定、有明确格式的信息,靠正则表达式识别,不依赖大模型,速度快、
可靠性高:

| 类型 | 识别方式 | 置信度 |
|---|---|---|
| 身份证号 | 18位数字(末位可为 X/x),用官方校验码公式验证 | 校验通过=高,校验不通过=低(仍然标记,不会因为校验失败就不处理——OCR 认错一位数字很常见) |
| 手机号 | `1[3-9]` 开头的 11 位数字 | 高 |
| 银行卡号 | 16-19 位连续数字(排除已被识别为身份证/手机号的部分) | 高 |
| 邮箱 | 标准邮箱格式 | 高 |
| 出生日期 | `YYYY年M月D日出生`——**必须带"出生"两个字才算**,普通日期(开庭日期、签约日期)不会被误伤 | 高 |
| 案号 | 标准法院案号格式,如`(2024)京0105民初12345号`、`(2023)最高法民申678号` | 高 |

**为什么身份证校验失败也照样标记**:OCR 识别扫描件时,经常把身份证号
里的某一位数字认错,认错了校验码就会不通过。如果因为校验不通过就
不处理,反而会把认错的真实身份证号漏掉——这是故意设计成"宁可多标记,
不可漏标记"。

### 2. 案件人员清单(源码:`core/lexicon_matcher.py`)

每个案件文件夹里的`案件人员清单.txt`,由律师手工填入本案涉及的姓名、
公司名。这是**整个工具里最可靠的一层**,原因很直接:范围小(只有律师
自己填的那几个名字)、精确匹配(不是靠猜),不存在识别错误的问题。

- 按字符串精确匹配全文,一个字都不差才算命中。
- 自动判断是"人名"还是"机构名":名单条目里如果包含"公司/集团/事务所/
  银行/医院/学校/协会/中心/厂"这些字,当机构处理,否则当人名处理。
  这个判断只影响脱敏后占位符显示的类型标签,不影响是否被脱敏。

**这层的可靠性完全取决于清单填得全不全**——这也是为什么每个新案件都
建议先花两分钟把涉案人名填进去,这两分钟比任何算法都管用。

### 3. 本地大模型识别(源码:`core/llm_detector.py`)

案件人员清单填不到的、事先不知道的姓名/机构/地址,靠本地大模型
(Ollama, qwen2.5:7b-instruct)识别。这是**唯一依赖 AI"猜"的一层**,
也是准确率最不稳定的一层,所以设计上:

- **只负责标出实体文字,绝不允许它改写原文**。模型返回的文字必须能在
  原文里逐字找到,找不到(模型编造/转述)的一律丢弃,不采信。
- **模型识别到的一切内容,一律标记为"低置信度"**,意味着只要大模型
  识别出任何东西,这份文档就一定会被拦截、要求人工审核——不会因为
  "大模型说了没问题"就自动导出。
- **长文档会被切成约 3000 字一段、留 200 字重叠**再分别识别。这是因为
  真实测试中发现过一次:一份 4.2 万字的 PDF 整篇一次性发给大模型时,
  模型直接放弃了"找实体"这个任务,改成输出了一份论文摘要——这是已经
  真实发生过的 bug,不是假设的风险。
- 大模型调用失败或返回格式不对时会重试,重试仍失败就整份判定为
  "这一层没跑成功",同样会拦截自动导出(不会当成"这一层什么都没
  找到")。

**已知的真实误报**(黄金语料回归测试跑出来的,不是猜测):大模型偶尔
会把"原告""被告""丙方"这类纯粹的法律角色称谓误判成人名。后果只是
候选文本里多出一两个不必要的占位符,不影响安全性(不会漏),但读起来
可能有点别扭。

### 4. 合并与替换(源码:`core/merge_replace.py`)

前三层各自给出的"这段文字是敏感信息"的判断,合并成最终的替换结果:

- **同一类型的重叠标记会合并**(避免同一段文字被重复替换);
  **不同类型的重叠标记永远不合并**——比如同一个位置一个检测器说是
  "人名"、另一个说是"机构名",两个判断都保留,不会因为位置重叠就
  抹掉一个,因为这代表两个检测器对同一处内容有分歧,该交给人工看。
- 同一个真实姓名/号码,不管出现多少次,替换成同一个占位符
  (如`⟦人名001⟧`),而且这个对应关系**跨同一案件的所有文档保持一致**
  ——第一份文档里"张三"是`⟦人名001⟧`,第二份文档里的"张三"还是
  `⟦人名001⟧`。
- 除了被替换的片段,原文其余部分**逐字不变**——大模型全程不参与这一步,
  这一步是纯粹的确定性程序逻辑,不会因为模型"发挥"而改变原文内容。

## 二、规则之外的强制审核机制(这部分同样重要)

以下几种情况,不管前面四层检测结果如何,都会拦截"一键自动导出",
要求先人工确认——因为这几类风险,靠"识别得更准"是解决不了的。

| 机制 | 拦截原因 |
|---|---|
| **处理台账**(源码:`core/orchestrator.py`) | 任何一页/一张嵌入图片在转换或 OCR 时失败(超时、文件损坏),都会被记录并拦截——不会把"没处理成功"悄悄当成"没有敏感信息"。 |
| **表格/表单检测**(源码:`core/table_detector.py`) | 表格里"姓名"和"身份证号"如果被错误拼接到相邻的人身上,每一项单独看都合法、置信度都正常,但对应关系是错的——这种错误规则和置信度都发现不了,只能强制人工核对整份内容。目前只能识别"每个单元格被独立区分"的表格,如果整行内容被合并识别成一整行文字,这个检测抓不到。 |
| **同名待确认**(源码:`core/case_workspace.py`) | 「案件人员清单」里同一个姓名出现不止一次,当作"本案可能有两个不同的人恰好同名"的信号——系统没有能力自动区分两个同名的人,统一替换成同一个占位符可能造成两个人的身份被混淆,所以拦截下来要求人工确认。 |
| **导出前独立复查**(源码:`core/leak_check.py`) | 在文件真正写入`03_已批准可上传`之前,重新扫描一遍最终文本:①所有已识别的真实敏感值,确认真的一个不剩;②对最终文本重新跑一遍正则检测,确认扫不出任何身份证/电话/银行卡模式。这一步不信任前面任何检测环节的结论,是全流程里唯一"不相信自己、重新验证一遍"的环节。 |
| **OCR 置信度审核**(源码:`core/ocr_vision.py`) | 扫描件/拍照卷宗,Vision 引擎逐行给出识别置信度,低于阈值(0.5,由实测校准)的行会连同原始页面图片一起展示在审核报告里,要求对照原图检查有没有认错字。 |
| **外部命令超时保护**(源码:`core/subprocess_utils.py`) | 损坏或超大的文件不会让程序卡死无响应。 |

## 三、目前已知覆盖不到的地方(如实列出,不回避)

- 法院/政府机关/银行名称目前会被一并脱敏,偏保守但可能过度。
- 大模型偶尔把"原告""被告"这类角色称谓当成人名(见上文第 3 层)。
- OCR 在真实手机拍照类卷宗上的表现,尚未用真实样本做过系统性测试
  (目前的回归测试语料都是纯文本)。
- 表格检测只能识别"单元格被独立区分"的情况,整行合并识别的表格
  抓不到。
- 两个人共用一个姓名时,系统本身没有能力自动区分身份——依赖"同名
  待确认"这个人工审核关卡兜底,不是靠算法解决的。

## 四、这份文档和代码的关系

以上规则均来自代码里的实际实现,不是凭空描述。如果以后代码有调整
(比如加了新的实体类型、调整了置信度阈值),这份文档需要同步更新,
否则会变成"文档说的和代码做的不一样"。
